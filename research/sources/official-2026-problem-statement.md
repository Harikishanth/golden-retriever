# Official 2026 problem — Business Entity Resolution

- **Captured:** 2026-09-25 05:11 IST
- **Source:** official Unstop problem video (~5:55) + user-provided **actual caption** (not OCR of slides)
- **Unstop page also showed:** 25 Sep 00:00 – 27 Sep 23:59 IST; 1–2 page + code zip; private LB after; next listed window **2 Oct 10:00–23:59 IST**
- **Slides OCR is unreliable** (words glued). Trust the caption below over slide screenshots.

This is **the** 2026 problem for the contest video. Do not treat it as a unique per-team statement unless Unstop literally serves a different PDF to this team.

---

## Caption (verbatim, user paste)

This year's problem is business entity resolution, a fundamental and widely encountered problem in real-world data. You will be given business records that arrive from three independent sources, each noisy and inconsistent, and the goal is to determine which of them describe the same real-world business. In this video, I will walk you through the problem statement, the dataset, the scoring, the submission format, and how entries are judged. Let's start with where this data comes from. Consider a business signing up on Amazon Business. At sign-up, we capture its core details, such as the business name and address. To build a richer picture of that same business, we pull in additional information from other data providers. Each of these sources typically comes from a different data vendor, with its own formats and conventions. The difficulty is that these external sources share no common identifier with ours, so the only fields we can rely on are the business name and address. For this challenge, we have deliberately limited the data to names and addresses. Here is the core difficulty: the same real-world business is described differently by each source. One vendor may write Acme Robotics Incorporated, another abbreviates the address, and a third references a nearby landmark. Source one is our clean, deduplicated reference list. Sources two and three are the noisy fragments that must be reconciled against it, and there is no shared identifier linking them. Entity resolution is the task of linking these records together, establishing that differently written records all refer to the same business. For every entity in source one, the goal is to find all of its matching records in sources two and three. A source one entity may match many records, exactly one, or none at all. Comparing every source one record against every source two and source three record would be far too expensive at scale. So we first apply blocking, sorting records into buckets using a cheap key built from both the name and the address, so that records likely to match land in the same bucket. Each color here is one such block. Notice that every record carries its own ID, a noisy name, and an address, and records can group either through a similar name or through a shared address. We follow the orange block, built around Acme Robotics, and the records in it become the candidate matches for that entity. Blocking favors recall, so the bucket also pulls in lookalikes: a business with a similar name at a different address, and a different business that happens to share an address. The matching model removes those in the next step. Blocking narrows an enormous number of possible comparisons down to a manageable set of candidate pairs. Finally, a matching model scores each candidate pair and keeps only the true matches, discarding the rest. That produces the two outputs of this challenge: the candidate pairs from your blocking stage and the final matching results, which is the final score on the leaderboard. You are provided with two datasets. The training set contains records across all three sources, together with the ground truth labels, a file that specifies for each source one business exactly which source two and source three records it matches. The test set contains the same three sources, but no labels, and this is what you generate predictions for. A note on the label format: the ground truth is stored as one row per source one entity. Its ID may map to a comma-separated list of all of its matching IDs, so a single row carries that entity's complete match set, and the list is empty when the entity matches nothing. This mirrors exactly what you submit. One practical reminder: all files are tab-separated, so read them with an explicit tab separator; otherwise the columns will not parse correctly. There are two deliverables. Throughout the challenge, you upload a single file, matching underscore results. tsv, containing one row per source one entity with its predicted matches. This is the only file scored on the leaderboard. At the close of the challenge, every team also submits a single archive. It contains the final matches, along with candidate underscore pairs. tsv, the candidate set your blocking stage produced before the model narrowed it down. This file is not scored, but it is used to audit the quality of your blocking. The archive also includes your complete, runnable pipeline and a methodology document describing your approach. The packages of the top teams are reviewed in detail before final rankings are confirmed. One reminder: run the provided validation script before submitting, so a simple formatting error does not cost you a submission. Finally, how entries are judged. Submissions are scored using the macro F0. 5 metric, which weights precision twice as heavily as recall. In practical terms, incorrectly merging two different businesses is penalized roughly twice as much as missing a true match. So when in doubt, it is safer not to merge. A few recommendations: first, understand singletons. A singleton is a source one entity that has no matching record in source two or source three. If you correctly predict an empty list for it, you earn a full score of one on that entity. If you predict any match, you score zero. So identifying the businesses with no match is just as important as finding the ones that do match. Second, your blocking strategy sets the ceiling on your recall you can achieve, so invest in it first, because you cannot match a record you never consider. And third, pay attention to region-specific patterns in both names and addresses. One firm rule: this is a pure machine learning challenge, so external databases, APIs, and lookups are strictly prohibited. Use only the provided data. That is the challenge. Build something you are proud of, resolve those entities, and enjoy the process. We are excited to see what you create. All the best.

---

## Named files (from video / slides, still confirm on disk)

| File | Role |
|---|---|
| `train_ground_truth.tsv` | one row per Source 1 id → comma-separated matching S2/S3 ids (empty = none) |
| `matching_results.tsv` | **only LB-scored file** during contest; same shape as GT |
| `candidate_pairs.tsv` | blocking output; **not** LB-scored; audited in final zip |
| `utils/validate_submission.py` | run before upload |
| `documentation_template.md` | methodology in final zip |

Exact record-table column names / id prefixes (`S1-…` on slides) = confirm by opening the TSVs. Read with `sep="\t"`.

---

## Unstop portal problem statement (verbatim structure)

`captured: 2026-09-25 05:25 IST` · `source: Unstop problem page` · **this text wins over the video** where they conflict (especially: `country` column, France in test, 8B/MIT-Apache, candidate_pairs = last pre-model set, private LB = final rank, methodology no page limit).

Download: same page → **Download Data Set / Student Resource**.

User paste stored in team chat / CONTEXT §3.0–§3.7. Full body as received 05:25 IST is the Unstop “Problem Statement / File Format / Data Description / Output Format / Final Submission Package / Constraints / Evaluation / Leaderboard / Fair Play / Tips” block. Do not re-summarize over the raw portal if they disagree — paste a new changelog row.
